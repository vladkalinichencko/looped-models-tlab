# Looped Models — много лупов без сатурации

## Задание

### О чём это

Мы занимаемся **looped-трансформерами**, где один и тот же блок из нескольких слоёв
применяется несколько раз. Такая архитектура позволяет не только делать больше
флопсов при меньшем количестве параметров, но и потенциально даёт inductive bias
к ризонингу внутри модели: она вынуждена меньше запоминать и больше вычислять, а
рекуррентная структура позволяет нативно реализовать итеративные алгоритмы.

Текущие методы сфокусированы на небольшом количестве лупов (например, 4 в Ouro или
2 в Loop the Loopies), и если лупить больше, приросты сатурируются (например, после
10 в Huginn). Также возрождается направление **DEQ**, в котором бэкпроп производится
через неподвижную точку, и следовательно приветствуется быстрая сходимость — а после
неё все вычисления становятся бессмысленными.

Мы видим в этом существенное ограничение на скейлинг флопсов на параметр, и, кажется,
можно придумать что-то лучше.

### Задача

Придумать метод, который позволит делать **много лупов**, чтобы из модели
фиксированного размера выжать как можно больше полезных вычислений.

Сделать претрен looped-трансформера на предсказание следующего токена на
[FineWeb](https://huggingface.co/datasets/HuggingFaceFW/fineweb):

| ограничение | значение |
|---|---|
| Параметры | $\le 10$M |
| Бюджет токенов | $\le 100$M |
| База архитектуры | Qwen3 (`modeling_qwen3.py`), с необходимыми модификациями |
| Количество лупов | чем больше, тем лучше |

Можно реализовать ранний выход из лупов (например, как Q-exit,
[arXiv:2204.03276](https://arxiv.org/abs/2204.03276)).

Задача — получить **как можно более низкую перплексию на валидации за счёт большого
количества лупов**. Добиться этого можно за счёт разных схем лупинга, других
нормализаций, exploration во время лупов и т.д. — рамки намеренно не задаются, все
design choices за вами.

### Критерии

Показать две вещи:

1. Вы умеете генерить хорошие идеи.
2. Вы умеете их реализовывать и проверять.

Вторая задача на первый взгляд проще — достаточно аккуратно сформулировать
постановку для кодинг-агента, и он всё сделает. Однако важно не теряться в коде,
который он пишет: следите за всем, что происходит в течение сессии. Современные
LLM умеют решать сложные открытые математические задачи, но при этом запросто
возьмут неправильный токенизатор или забудут сохранить чекпойнт.

Для первого критерия готового рецепта нет. Есть только совет: используйте
изначальный недостаток знаний в свою пользу — сначала подумайте, как подойти к
задаче, и набросайте идеи на своём бэкграунде; если сразу броситься читать статьи
по теме, вы окажетесь в той же лодке, что и все остальные в этой области. Также
старайтесь не использовать LLM для генерации идей — они пока не умеют это хорошо.

### Оформление решения

Код и отчёт (TeX или markdown) — в репозитории на GitHub, лучший чекпойнт — в
открытый репозиторий на Hugging Face. В коде должны быть **как минимум** скрипты
для запуска обучения и эвала обученного чекпойнта. Отчёт должен содержать подробное
описание всех экспериментов и финальной архитектуры с анализом того, почему именно
такой вид даёт лучшие результаты.

Кроме того, желательно обосновать, **почему метод будет работать хорошо и на большем
скейле**. Так, например, добавить большую обучаемую матрицу value embeddings — плохо,
потому что польза от неё быстро умрёт при скейлинге параметров и бюджета обучения.

> Примечание: отсутствие положительного результата при хорошем анализе всех
> негативных — хороший результат.

### Примеры looped-моделей

- Ouro — [arXiv:2510.25741](https://arxiv.org/abs/2510.25741)
- EBT — [arXiv:2507.02092](https://arxiv.org/abs/2507.02092v1)
- Huginn — [arXiv:2502.05171](https://arxiv.org/abs/2502.05171)
- DEQ — [arXiv:1909.01377](https://arxiv.org/abs/1909.01377)
- Q-exit / PonderNet-подобный ранний выход — [arXiv:2204.03276](https://arxiv.org/abs/2204.03276)

---

## Сетап

| | |
|---|---|
| Данные | `HuggingFaceFW/fineweb`, сэмпл `sample-10BT`, стрим |
| Токенизатор | `Qwen/Qwen3-0.6B` (дефолт) — **один и тот же во всех прогонах** |
| Бюджет | $\le 100$M токенов на прогон, одинаковый у всех вариантов |
| Метрика | val perplexity на фиксированном held-out шарде |

Честное сравнение: у looped-модели с $L$ лупами и у бейзлайна должно совпадать
**всё**, кроме схемы лупинга — токены, шедулер, seed, размер словаря.

### Решить до первого прогона: что считаем за 10M параметров

Словарь Qwen3 — $151\,936$ токенов. При $d_{\text{model}} = 256$ одни только
(связанные) эмбеддинги дают $151936 \times 256 \approx 38.9$M параметров, то есть
бюджет выбран целиком ещё до первого блока. Варианты:

1. Считать **non-embedding** параметры (так делают в большинстве работ по скейлингу);
2. Взять словарь поменьше ($\approx 16$k, свой BPE на FineWeb) — тогда обе трактовки
   выполняются одновременно.

**Решено 2026-08-20: вариант 2.** `python data.py tokenizers/fineweb16k` обучает
свой ByteLevel-BPE на 120k документов FineWeb. Тогда обе трактовки лимита выполняются
одновременно и не нужно оправдываться в отчёте. Побочный выигрыш крупнее самого
спора: при словаре 152k каждый шаг платил за `lm_head` шириной 152k, то есть большая
часть флопсов уходила не в блоки, которые мы и изучаем.

`train.py` печатает обе цифры и предупреждает при превышении.

### Сверка с первоисточниками: что я делал не по статьям

Проверил каждую реализованную идею по её статье, а не по конспекту. Расхождения были
почти везде.

#### Huginn (arXiv:2502.05171)

| | в статье | было у меня | стало |
|---|---|---|---|
| инъекция входа | **конкатенация** $[s_i, e]$ и адаптер $A:\mathbb R^{2h}\to\mathbb R^h$ | сложение $h + h_0$ | оба, `--input-injection concat\|add` |
| что инжектится | выход **prelude** | сами эмбеддинги | выход prelude |
| структура | prelude / core / coda, повторяется только core | повторялось всё | `--n-prelude`, `--n-coda` |
| число повторов | $\tau \sim \mathcal N(\log\bar r - \tfrac{\sigma^2}2, \sigma)$, $r \sim \mathrm{Pois}(e^\tau)+1$, $\bar r = 32$ | равномерное | `--loop-sampling lognormal` |
| бэкпроп | **усечённый**, последние $k=8$ шагов | через все | `--backprop-last` |
| ранний выход | **без обучаемой головы**: $\mathrm{KL}$ между соседними шагами $< 5\cdot10^{-4}$ | обучаемая голова | `forward(exit_kl=...)` |

Про усечённый бэкпроп стоит сказать отдельно: **это то, чем 32 повтора вообще делаются
обучаемыми** — память не растёт с числом повторов. Я вместо этого прикрутил
`--grad-checkpoint`, который решает ту же задачу дороже и не так, как в статье.

Про ранний выход — тоже: Huginn обходится **без обучения вовсе**, выходя когда
предсказание перестало меняться. У меня KL между шагами уже считался в диагностике, и
надо было заметить, что это готовый критерий.

#### Ouro (arXiv:2510.25741)

| | в статье | было у меня |
|---|---|---|
| архитектура | $H^{(t)} = \mathrm{TransformerLayer}_\theta(H^{(t-1)})$, **инъекции входа нет** | совпадает: это моя базовая строчка |
| лосс остановки | $\mathcal L = \sum_t q_\phi(t\lvert x)\,\mathcal L^{(t)} - \beta\,H(q_\phi)$ | PonderNet: KL к геометрическому приору |
| регуляризатор | **энтропия самого распределения**, её максимизируют | приор с фиксированной $\lambda$ |
| выход на инференсе | $\mathrm{CDF}(t\lvert x) \ge q$ | по $\lambda$ самой по себе |
| $\beta$ | 0.1 в начале, 0.05 дальше | 0.01 |
| шагов при обучении | **4**, снижено с 8 «для устойчивости» | 4 |

Последняя строка — важная для отчёта: **главная статья по лупам обучает на четырёх
повторах и явно пишет, что снизила с восьми ради устойчивости.** Условие просит «чем
больше лупов, тем лучше», и это прямо противоречит тому, что смогли сделать авторы.

Оба варианта остановки оставлены (`--early-exit ouro|pondernet`), чтобы измерить, что
даёт замена регуляризатора.

#### PALBERT / Q-exit (arXiv:2204.03276)

Условие называет это «Q-exit», и я до сверки считал, что моя голова остановки — оно.

| | в статье | было у меня | стало |
|---|---|---|---|
| вход lambda-слоя | **пара соседних состояний** $[h_i, h_{i-1}]$ | одно текущее $h$ | пара |
| архитектура | **трёхслойный MLP с tanh** | один Linear | трёхслойный с tanh |
| критерий выхода | $\sum_{i\le j} p(i\lvert x) \ge q$ | по $\lambda$ самой по себе | по CDF |
| $\beta$ у KL | **0.5** | 0.01 | 0.5 |

И отдельно, чтобы не приписывать себе чужое и наоборот: **«голова прогресса» — не из
PALBERT.** Там $\lambda$ предсказывает вероятность остановки. Предсказывать, сколько
лосса снимет следующий шаг, — это развитие их наблюдения о том, что голове полезна
динамика состояния, а не само состояние; в коде так и подписано.

### Сверка с настоящим Qwen3-0.6B

Условие говорит «за основу взять архитектуру Qwen3». Сверился с конфигом
`Qwen/Qwen3-0.6B`, а не с памятью, и нашёл четыре расхождения — все исправлены.

| | Qwen3-0.6B | было у меня | стало |
|---|---|---|---|
| `head_dim` | **128, задан независимо**; $n_{\text{heads}}\cdot d_{\text{head}} = 2 d_{\text{model}}$ | $d/n_{\text{heads}}$ | 128, независимо |
| внимание | **GQA 2:1** (16 голов, 8 kv) | MHA | GQA 2:1 |
| $d_{\text{ff}}/d_{\text{model}}$ | **3.0** | 1.75 | 3.0 |
| `rope_theta` | **1 000 000** | 10 000 | 1 000 000 |
| `rms_norm_eps` | 1e-6 | 1e-6 | без изменений |
| biases, tied embeddings, SiLU | нет / да / да | совпадало | без изменений |

Первое расхождение самое существенное: у Qwen3 внимание проецирует запросы в
**вдвое более широкое** пространство, чем поток невязки, а ключи и значения — в
ровно такое же. Это не мелочь оформления, это форма блока.

### Конфигурация под 10M без эмбеддингов

Размеры подобраны так, чтобы **все отношения Qwen3 сохранились точно**, а в бюджет
влезли четыре слоя:

| | |
|---|---|
| $d_{\text{model}}$ | 384 |
| голов | 6, из них kv 3 (**GQA 2:1**) |
| $d_{\text{head}}$ | 128, то есть $6\cdot128 = 768 = 2 d_{\text{model}}$ |
| $d_{\text{ff}}$ | 1152 $= 3 d_{\text{model}}$ |
| слоёв в блоке | 4 |
| словарь | 16384 |
| **параметров без эмбеддингов** | **8.85M** |
| всего | 15.14M |

Четыре слоя, а не два, потому что схема «группами» ($f_1f_2\,f_1f_2\,f_3f_4\,f_3f_4$)
на двух слоях вырождается в схему «стеком».

Параметры на слой при таких отношениях: внимание $6d^2$ (запросы $2d^2$, ключи и
значения по $d^2$, выход $2d^2$), MLP $9d^2$, итого $15d^2$ — при $d = 384$ это
2.21M, четыре слоя дают 8.85M.

### Ещё одна деталь рецепта: распад весов не на всём

`AdamW` применялся ко **всем** параметрам, включая одномерные веса нормализаций. Для
них распад — не регуляризация, а медленный сдвиг масштаба активаций, и в рецептах
обучения языковых моделей их всегда выносят в отдельную группу. Исправлено: распад
только у параметров с двумя и более измерениями.

### Инициализация масштабируется по числу применений блока

Стандартный приём GPT-2 — делить выходные проекции на $\sqrt{2L}$, где $L$ — глубина.
Здесь глубина это **число применений блока за проход**, а не число блоков: при 16
лупах в поток невязки пишут 16 раз одни и те же веса. Без поправки $\lVert h\rVert$
на выходе растёт тем сильнее, чем больше лупов, и сравнение схем превращается в
сравнение масштабов активаций. С поправкой на инициализации $\lVert h\rVert$
одинаково стартует у всех вариантов.

## Лог

**2026-08-16 — сетап, пайплайн проверен на игрушечном бюджете.**

- Прогон `smoke`: 100k токенов, $d=256$, 2 слоя в блоке, 4 лупа, batch 4 $\times$ 256,
  MPS. Loss 11.99 (ровно $\ln 151936$ на инициализации) $\to$ 7.9, чекпойнт сохранён,
  метрики в MLflow.
- **Параметры: 40.43M всего / 1.61M без эмбеддингов.** Словарь Qwen3 (151936) при
  $d = 256$ даёт 38.9M связанных эмбеддингов — бюджет в 10M выбран до первого блока.
  Решение из раздела «Сетап» надо принять до серьёзных прогонов.
- Второе следствие того же словаря, менее очевидное: **логиты дороже всей модели**.
  При batch $16 \times 512$ выход `lm_head` это $8192 \times 151936 \approx 1.2$ млрд
  чисел за шаг — обучение упирается в него, а не в блоки. Пришлось уронить батч до
  $4 \times 256$, чтобы шаг шёл за секунду. Словарь на 16k убирает обе проблемы разом.
- Свип по числу лупов на этом чекпойнте (`eval.py --loops 1 2 4 8 16`):

| лупов | 1 | 2 | 4 | 8 | 16 |
|---|---|---|---|---|---|
| val ppl | 3478 | 3503 | 3623 | 3714 | 3764 |

Модель обучена с 4 лупами, но лучший результат даёт **один** луп, дальше монотонно
хуже. На 100k токенов (в 1000 раз меньше бюджета) это ничего не доказывает, но задаёт
вопрос, ради которого всё задание: рекуррентность пока не даёт ничего, лишние
итерации просто уводят представление. Повторить на полном бюджете **до** того, как
изобретать схемы лупинга.

- MPS: для этой модели считает нормально. В соседнем проекте один прогон на MPS упал
  с NaN, но воспроизвести не удалось, и падение совпало по времени с этим обучением —
  **два процесса на MPS одновременно лучше не запускать**. Финальные числа всё равно
  считать на CUDA.

## Идеи

<!-- сюда свои идеи ДО чтения статей — это прямо просят в условии -->

## Результаты

**Бейзлайн 2026-08-17.** Бюджет 2M токенов, $d=256$, 2 слоя в блоке, batch $4\times256$,
seed 0, токенизатор Qwen3. Отличается **только** число лупов.

| вариант | лупов | нормализация между лупами | val ppl |
|---|---|---|---|
| `dense1` | 1 | — | **1019.4** |
| `loop4` | 4 | — | 1245.7 |
| `loop8` | 8 | — | (считается) |
| `loop4_norm` | 4 | RMSNorm | 2646.4 |
| `loop8_norm` | 8 | RMSNorm | 2632.9 |

Параметров везде одинаково: 40.43M всего / 1.61M без эмбеддингов.

**Наивный лупинг при равном бюджете токенов делает хуже.** Это ожидаемая стартовая
точка, а не ошибка: лупнутая модель тратит вчетверо больше вычислений на шаг, но за
те же 2M токенов сходится медленнее. Именно этот разрыв и предлагается закрыть.

Оговорка для отчёта: сравнение при равном **бюджете токенов** (как требует условие) не
то же самое, что при равном **бюджете вычислений**. При равных флопсах dense успел бы
увидеть вчетверо больше токенов, и разрыв был бы ещё больше. Стоит показать обе оси.

## Открытые вопросы

<!-- что не выяснено; убирать отсюда только вместе с ответом в «Лог» -->

- Почему нормализация между лупами так сильно ломает? Проверить гипотезу про масштаб:
  обучить с `loop_norm`, но подобрать масштаб финального `norm` под новый режим.
- Три альтернативы из раздела выше (нормировка обновления, обучаемый гейт, инъекция
  входа) — ни одна не проверена.
- Эффективный ранг 13–17 из 256. Это узкое место или так и надо? Сравнить с рангом у
  dense-модели того же размера на большем бюджете.
- Все выводы получены на 2M токенов вместо 100M. Что из них выживет на полном бюджете?
- Словарь: решение «считать non-embedding» или «свой BPE на 16k» так и не принято.

## Диагностика лупов (2026-08-18)

`tmp/diagnose_loops.py` меряет по каждому лупу: норму состояния, относительный шаг,
косинус между сдвигом и антиградиентом лосса, эффективный ранг, KL до финального
распределения и лосс, если читать логиты прямо сейчас.

**`loop4` (обучен с 4 лупами), 12 лупов на инференсе:**

| луп | ‖h‖ | шаг/‖h‖ | cos(dh, −dL/dh) | эфф. ранг | лосс |
|---|---|---|---|---|---|
| 1 | 1553 | 422.5 | +0.046 | 13.2 | 8.010 |
| 2 | 1914 | 0.314 | **+0.058** | 14.5 | 7.472 |
| 3 | 2242 | 0.218 | +0.022 | 15.1 | 7.399 |
| 4 | 2548 | 0.169 | +0.008 | 15.6 | **7.389** |
| 5 | 2837 | 0.139 | +0.001 | 15.9 | 7.397 |
| 6 | 3114 | 0.119 | −0.004 | 16.2 | 7.413 |
| 8 | 3643 | 0.094 | −0.010 | 16.6 | 7.455 |
| 12 | 4645 | 0.069 | −0.019 | 17.1 | 7.571 |

Три вывода, каждый проверяемый:

1. **Полезная работа кончается ровно там, где кончилось обучение.** Косинус с
   антиградиентом положителен на лупах 1–5 и переходит через ноль на шестом; минимум
   лосса — на четвёртом, то есть ровно на числе лупов, с которым модель обучалась.
   Дальше лупы двигают состояние **против** направления, уменьшающего лосс. Это не
   «бесполезно», это вредно, и теперь измерено, а не предположено.

2. **Насыщение здесь — не сходимость к неподвижной точке.** Относительный шаг падает
   (0.31 → 0.07), но абсолютный почти не меняется: 0.314 × 1553 ≈ 490 против
   0.069 × 4398 ≈ 304. Падает не скорость движения, а её доля от нормы, потому что
   **‖h‖ растёт без ограничения — примерно +250 за луп, линейно**. Состояние не
   стабилизируется, оно уезжает наружу, а каждый шаг всё меньше на этом фоне.
   Pre-norm блок не видит роста h, поэтому и не тормозит.

3. **Эффективный ранг крошечный: 13–17 из 256 измерений.** Лупы его почти не
   расширяют. У `dense1` при этом 25–34, то есть лупнутая модель использует **вдвое
   меньше** направлений. Похоже на выученную низкоранговую траекторию.

Для сравнения `dense1` (обучен одним лупом): косинус уже на втором лупе +0.003, с
третьего отрицательный, лосс растёт монотонно с 7.24. Ожидаемо — модель не училась
итерировать.

**Гипотеза, которая прямо следует из пункта 2:** ограничить рост нормы между лупами.
Если насыщение вызвано разбуханием h, а не сходимостью, то нормализация состояния
после каждого лупа должна вернуть лупам возможность работать. Проверяется одним
флагом и прогоном на том же бюджете.

### Проверка гипотезы про норму: опровергнута

`loop4_norm` — то же самое, что `loop4`, плюс RMSNorm после каждого лупа. Бюджет,
сид, всё остальное совпадает.

| вариант | val ppl |
|---|---|
| `loop4` | **1245.7** |
| `loop4_norm` | 2646.4 |

**Стало вдвое хуже.** Наблюдение про рост нормы было верным, но лечение неверное.

Объяснение, которое стоит проверить отдельно: резидуальный поток накапливает
информацию **в том числе в масштабе**. Каждый блок дописывает свой вклад, и величина
этого вклада относительно текущей нормы — это и есть «насколько сильно данный луп
вмешался». RMSNorm после каждого лупа стирает эту величину: состояние принудительно
возвращается к единичному масштабу, то есть каждый луп начинает с чистого листа и
накопление ломается. Плюс финальный `norm` и `lm_head` обучались под другой масштаб.

Что стоит попробовать вместо тупой нормализации:

1. **Нормировать не состояние, а обновление**: $h \leftarrow h + \gamma \cdot \frac{f(h)}{\lVert f(h)\rVert}$ — рост станет линейным и контролируемым, но накопление сохранится.
2. **Обучаемый гейт**: $h \leftarrow (1-g)\,h + g\,f(h)$, где $g$ предсказывается по состоянию. Это и ограничивает рост, и даёт модели механизм «хватит».
3. **Инъекция входа** (как в Huginn): $h \leftarrow f(h + h_0)$ — не даёт забыть исходный токен, при этом масштаб не трогает.

Вывод для отчёта: разбухание нормы — симптом, а не причина. Дальше надо отличать
«норма растёт, потому что модель накапливает» от «норма растёт бесконтрольно».

## Глубокая диагностика: что именно делает луп

`tmp/diag_deep.py`, одна фиксированная последовательность (26 токенов), CPU.

### Градиент через лупы не затухает

Бэкпроп от финального лосса, норма $\partial L/\partial h$ на каждой границе лупа:

| | эмбеддинги | луп 1 | луп 4 | луп 8 | луп 11 |
|---|---|---|---|---|---|
| `loop4` | 4.5e-1 | 1.0e-3 | 7.6e-4 | 7.8e-4 | 1.2e-3 |
| `dense1` | 8.9e-3 | 5.7e-3 | 2.0e-3 | 1.1e-3 | 1.3e-3 |

Провал в 500 раз есть только **между эмбеддингами и первым лупом**; дальше норма
держится ровно и с шестого лупа даже растёт (коэффициент 1.02 → 1.20 за луп).
**Затухание градиента насыщение не объясняет** — сигнал обучения доходит до всех
лупов примерно одинаково. Эту гипотезу можно закрыть.

### Предсказание решается на первом лупе

| | энтропия, нат | вероятность топ-1 | медиана лупа, где топ-1 замер | застыло после 1 лупа |
|---|---|---|---|---|
| `loop4` | 8.02 → 7.07 | 0.097 → **0.082** | **1** | **17 из 26 позиций** |
| `dense1` | 7.18 → 6.12 | 0.120 → 0.178 | 8 | 0 из 26 |

У `loop4` две трети позиций фиксируют ответ уже после первого лупа, а средняя
уверенность с лупами **падает**. То есть последующие лупы для большинства токенов не
вычисляют ничего нового, только размывают ответ.

Любопытно, что у `dense1` (обучен одним лупом) картина обратная: ни одна позиция не
замирает сразу, уверенность растёт 0.12 → 0.18, топ-1 стабилизируется к 8-му лупу.
Модель, которую **не** учили лупить, при лупинге ведёт себя больше похоже на
итеративное уточнение, чем модель, которую учили. Это стоит перепроверить на большем
бюджете — если подтвердится, это сильное наблюдение.

### Луп — это не уточнение, а дрейф по прямой

Из одного состояния делаем один лишний проход и два, сравниваем сдвиги:

| старт | лосс(h) | +1 | +2 | ‖шаг1‖ | ‖шаг2‖ | **cos(шаг1, шаг2)** |
|---|---|---|---|---|---|---|
| 1 | 7.961 | 7.348 | 7.199 | 2636 | 2225 | **0.989** |
| 2 | 7.348 | 7.199 | 7.172 | 2225 | 2014 | **0.993** |
| 4 | 7.172 | 7.179 | 7.200 | 1883 | 1791 | **0.996** |
| 8 | 7.255 | 7.284 | 7.311 | 1620 | 1584 | **0.998** |

**Косинус между последовательными шагами 0.99.** Каждый следующий проход двигает
состояние почти строго туда же, куда предыдущий, чуть меньшим шагом. Это не итерации
метода Ньютона, которые подкручивают ответ, а **движение по почти прямой линии с
медленно убывающим шагом** — то есть блок прибавляет практически фиксированное
направление.

Это сходится с наблюдением про линейный рост $\lVert h\rVert$ и объясняет всё
остальное: пользы от лупов нет не потому, что состояние сошлось, а потому что оно
едет по прямой и рано или поздно уезжает из области, где `lm_head` работает.
Улучшение лосса прекращается ровно на четвёртом лупе — там, где кончилось обучение.

**Отсюда конкретная цель метода: заставить последовательные шаги быть
неколлинеарными.** Всё, что даёт лупу знать, на каком он шаге (позиционное
обусловливание лупа, разные веса по фазам, шум/exploration между лупами), бьёт прямо
в этот механизм.

### Как MLP гнёт пространство

`tmp/warp_loop4.png`: сетка в плоскости PC1–PC2, четыре повторных применения
MLP-подслоя (берём MLP, потому что он поточечный; внимание так нарисовать нельзя —
оно смешивает позиции). Видно постепенное сжатие вдоль одной складки: деформация
накапливается монотонно в одном и том же месте, а не перестраивает пространство
заново на каждой итерации. Та же коллинеарность, только глазами.

## Перезапуск на честной конфигурации (2026-08-20)

Всё, что ниже в разделах «Диагностика» и «Важно: шум прогона», получено на старой
модели: словарь Qwen3, $d=256$, 2 слоя, 1.61M параметров без эмбеддингов. Числа
перплексии оттуда **несравнимы** с новыми — токенизатор другой. Качественные выводы
про геометрию (дрейф по прямой, косинус 0.99, невозрастание градиента) проверяются
заново на новых прогонах.

### Сколько это стоит на этой машине

`tmp/bench.py`, M1 Max, batch 16 × 512, при трёх параллельных прогонах UED на CPU:

| лупов | тыс. токенов/с | 25M токенов | 100M токенов |
|---|---|---|---|
| 1 | 20.1 | 0.35 ч | 1.4 ч |
| 2 | 11.3 | 0.61 ч | 2.5 ч |
| 4 | 6.0 | 1.15 ч | 4.6 ч |

Стоимость линейна по числу лупов, как и должно быть. Полный свип 1/2/4/8/16 на 100M
токенов — это порядка 36 часов только на нём одном. Поэтому **бюджет свипа 25M
токенов из разрешённых 100M**, одинаковый у всех вариантов; сравнение честное,
абсолютные ppl — нет. Финальная конфигурация гоняется на полном бюджете.

### Три схемы обхода, один компьют

`model.loop_plan` строит порядок применения блоков:

| схема | порядок при 4 слоях и 2 лупах |
|---|---|
| `stack` | $f_1f_2f_3f_4\;f_1f_2f_3f_4$ — так делают Huginn и Ouro |
| `layer` | $f_1f_1\;f_2f_2\;f_3f_3\;f_4f_4$ |
| `group` | $f_1f_2\;f_1f_2\;f_3f_4\;f_3f_4$ |

Число применений блока одинаково, различается только порядок, — то есть это
сравнение при равном компьюте и равных параметрах.

### Диагностика теперь ходит по тому же коду, что и обучение

`LoopedLM.forward` выражен через генератор `trace`, который отдаёт $h$ после каждого
шага плана; `diag.py` считает всё по этому генератору. Прошлый `tmp/diagnose_loops.py`
воспроизводил цикл руками и **игнорировал `input_injection` и `loop_norm`** — то есть
на этих чекпойнтах мерил не ту модель, что обучалась. Такое расхождение невозможно
заметить по числам, поэтому его убрали структурно.

`train.py --diag-every N` пишет всю таблицу в `runs/<tag>/diag.jsonl` по ходу
обучения. `viz.py` собирает из этого одну интерактивную страницу: куб «метрика ×
шаг лупа × момент обучения × прогон» режется ползунком, а не превращается в
двадцать статичных картинок.

### Первая точка свипа: `loops1`, 25M токенов

| | |
|---|---|
| параметров без эмбеддингов | 9.70M |
| всего | 18.09M |
| **val ppl** | **150.4** |

Это плотная модель из четырёх слоёв, обученная одним проходом, — точка отсчёта при
равных параметрах и равных токенах.

Прогон её же на большем числе проходов, чем она видела при обучении
(`eval.py runs/loops1/ckpt.pt --loops 1 2 4 8 16`):

| проходов | ppl |
|---|---|
| 1 | **150.4** |
| 2 | 208.7 |
| 4 | 610.4 |
| 8 | 1763.7 |
| 16 | 3283.1 |

Монотонная деградация: даром лишние проходы не дают ничего, чего и следовало ждать.
Интереснее, что деградация **плавная** — второй проход стоит 39% перплексии, а не
разрушает модель. Это согласуется с картиной дрейфа: лишний проход толкает состояние
дальше вдоль того же направления, а финальная RMSNorm частично съедает разницу
масштаба. Модель не «ломается», она уезжает.

### Два лупа обгоняют один, но только после пересечения на 20M токенов

`loops1` 150.42 против `loops2` **146.16** при одинаковых 24 985 600 токенах и
одинаковых 9.70M параметров без эмбеддингов. Разница 2.8% — сама по себе на грани
того, что можно утверждать. Но интересно не конечное число, а вся пара кривых:

| шаг | `loops1` | `loops2` | разница |
|---|---|---|---|
| 250 | 648.5 | 712.1 | **+63.6** |
| 500 | 413.8 | 444.2 | +30.4 |
| 1000 | 288.6 | 302.6 | +14.0 |
| 1500 | 223.4 | 231.8 | +8.4 |
| 2000 | 183.5 | 186.3 | +2.8 |
| 2250 | 172.8 | 174.1 | +1.3 |
| **2500** | 163.2 | 162.2 | **−1.1** |
| 2750 | 155.5 | 152.5 | −3.0 |
| 3050 | 150.4 | **146.2** | **−4.3** |

**Разница убывает монотонно все тринадцать точек и меняет знак около шага 2500, то
есть на 20M токенов.** Дальше разрыв растёт.

Монотонный тренд по тринадцати парным точкам — куда более сильное утверждение, чем
одно конечное число: разброс прогона так себя не ведёт. Читается это так: лупнутая
модель **тяжелее учится в начале** (при тех же параметрах эффективная глубина вдвое
больше), но **имеет более высокий потолок**, и на 25M токенов мы застаём её сразу
после пересечения, когда разрыв ещё только начал расти.

Два следствия, оба важные.

**Про короткие прогоны.** До 20M токенов честный вывод был бы «лупы хуже», и он был
бы неверным. Любая отбраковка идей по перплексии на укороченном бюджете здесь даст
обратный знак — ровно как в UED, где на 10% бюджета впереди оказывается DR.

**Про скейлинг.** Это и есть аргумент, который просит условие: разрыв не постоянный,
он растёт с бюджетом токенов. Обе кривые на конце бюджета всё ещё падают, то есть ни
одна модель не сошлась, и 100M токенов должны развести их сильнее. Проверяется прямо:
`TOKENS=100000000 ./run_sweep.sh 1 2`.

### Насыщение наступает ровно на обучающей глубине — и это проверяемое утверждение

`diag.py runs/loops2/ckpt.pt --loops 8`, то есть модель, обученная двум лупам,
прогнана на восьми. Рядом — контроль `loops1` из раздела выше.

| шаг | $\cos(\Delta h, -\partial L/\partial h)$ | лосс | эфф. ранг | $\rho(J)$ |
|---|---|---|---|---|
| 1 | +0.138 | 5.558 | 237.7 | 2146.0 |
| **2** | **+0.043** | **5.200** | 245.5 | 2.34 |
| 3 | **−0.003** | 5.282 | 242.6 | 2.35 |
| 4 | −0.019 | 5.440 | 237.8 | 1.92 |
| 6 | −0.034 | 5.812 | 227.6 | 1.53 |
| 8 | −0.041 | 6.178 | 218.0 | 1.27 |

**Знак $\cos$ переходит через ноль ровно после второго шага — то есть ровно на той
глубине, на которой модель обучалась.** До неё лосс падает (5.558 → 5.200), после
неё растёт (5.282 → 6.178).

Второе отличие от контроля: **ранг не схлопывается**. У `loops1` он падал 243.7 →
130.0 за восемь повторов, здесь держится 237.7 → 218.0. Обучение лупам сохраняет
занятые направления, даже когда шаги уже вредят.

Третье: $\rho(J)$ по-прежнему **больше единицы везде** (2.34, 2.35, 1.92, …, 1.27).
Обучение лупам не сделало отображение сжимающим и не создало неподвижной точки. Оно
изменило другое — куда направлен шаг, а не сходится ли последовательность.

#### Предсказание, записанное до `loops4` и `loops8`

Если насыщение — это свойство **обучающей глубины**, а не блока, то у `loops4`
косинус должен перейти через ноль после четвёртого шага, а у `loops8` — после
восьмого. Тогда «сатурация после десяти повторов у Huginn» — утверждение про то, как
Huginn обучался, а не предел архитектуры, и правильный вывод из задания —
не «придумать метод против сатурации», а **обучать на той глубине, которую хотим
использовать**, чего Huginn и добивается случайным числом повторов.

Если же переход через ноль окажется на одном и том же шаге у всех вариантов — это
свойство блока, и тогда нужен именно метод. Свип ответит на это за несколько часов, и
ответ будет один из двух.

### Обучение двум лупам делает блок устойчивым к лишней глубине

Каждый чекпойнт прогнан на разном числе проходов (`eval.py --loops 1 2 4 8 16`):

| проходов | `loops1` (обучена 1) | `loops2` (обучена 2) |
|---|---|---|
| 1 | **150.4** | 214.4 |
| 2 | 208.7 | **146.2** |
| 4 | 610.4 | **192.0** |
| 8 | 1763.7 | **436.3** |
| 16 | 3283.1 | **1142.3** |

Три вещи.

**Каждая модель лучше всего на своей глубине.** Рассогласование обучения и инференса
работает в обе стороны: `loops2`, прогнанная одним проходом, даёт 214 — хуже и своих
146, и чужих 150.

**Обучение всего двум лупам делает блок втрое устойчивее к лишним повторам.** На
четырёх проходах 192 против 610, на шестнадцати 1142 против 3283. Блок учится
переносить повторение сам по себе, а не только на той глубине, которую видел.

**Но даром глубина всё равно не даётся.** `loops2` на четырёх проходах (192) хуже
себя же на двух (146). Деградация замедляется втрое, но остаётся монотонной за
пределами обучающей глубины.

Отсюда прямой вывод для метода: «чем больше лупов, тем лучше» **не достигается
прогоном большего числа лупов на инференсе** — модель должна быть обучена на этой
глубине. Это ровно то, зачем Huginn обучается со случайным числом повторов на каждом
батче. Такой вариант — следующий кандидат, но его архитектуру показываю до
реализации, а не после.

### Обучение лупам переворачивает знак — видно уже на четверти прогона

`loops2`, снимок диагностики на шаге 750 из 3051 (`runs/loops2/diag.jsonl`), рядом с
контролем — `loops1`, прогнанным на двух проходах:

| | луп 1 | луп 2 |
|---|---|---|
| **`loops2`**, $\cos(\Delta h, -\partial L/\partial h)$ | **+0.245** | **+0.034** |
| `loops1` (контроль), то же | +0.136 | −0.002 |
| **`loops2`**, лосс при выходе | 6.053 | **5.967** |
| `loops1` (контроль), лосс при выходе | 5.19 | 5.44 |
| **`loops2`**, $\cos(\Delta h_2, \Delta h_1)$ | — | **0.653** |
| `loops1` (контроль), то же | — | 0.649 |
| **`loops2`**, эффективный ранг | 106.4 | **119.3** |
| `loops1` (контроль), то же | 243.7 | 229.0 |

Три вещи меняют знак разом. У обученной лупам модели второй шаг **полезен**
($\cos > 0$), лосс при выходе **падает** между лупами, а ранг **растёт**, а не
схлопывается. У контроля всё три наоборот.

Значит диагностика различает «лупить умеет» и «лупить не умеет», а не меряет общее
свойство архитектуры. Это и было условием, при котором ею вообще можно пользоваться
как критерием.

Второе, что видно сразу и относится прямо к теме задания: **первый луп делает
основную полезную работу** ($\cos = +0.245$), второй — в семь раз меньше
($+0.034$). Сатурация не «наступает после десятого повтора», она есть уже на втором,
просто пока в плюс. Дальше по свипу станет видно, где $\cos$ переходит через ноль —
это и будет измеренная точка насыщения, а не пересказ чужого графика.

### Оговорка про режим, в котором мы меряем

Бюджет условия — до 10M параметров и до 100M токенов, то есть не больше 10 токенов на
параметр; на нашем свипе 25M токенов это 2.5. Шиншилла-оптимум около 20. Мы меряем в
**недообученном** режиме, а в нём добавка параметров стоит дешевле добавки компьюта:
модель ещё не выжала то, что уже может запомнить.

Лупинг же добавляет ровно компьют и не добавляет параметров. То есть его просят
доказать свою полезность в режиме, наименее для него выгодном. Это не отговорка на
случай отрицательного результата — это то, что надо померить: если лупы выигрывают
даже здесь, аргумент про скейлинг сильнее обычного; если проигрывают, надо показать,
что разрыв сокращается с ростом бюджета токенов, и это отдельный эксперимент
(одна конфигурация на 25M / 50M / 100M).

### Что делает лишний проход с состоянием: `loops1` при восьми проходах

`python diag.py runs/loops1/ckpt.pt --loops 8`. Модель обучена одним проходом, то есть
это **контроль**: так выглядит блок, которого лупить не учили.

| шаг | $\lVert h\rVert$ | $\lVert\Delta h\rVert/\lVert h\rVert$ | $\cos(\Delta h_t, \Delta h_{t-1})$ | $\cos(\Delta h, -\partial L/\partial h)$ | эфф. ранг | $\rho(J)$ | лосс |
|---|---|---|---|---|---|---|---|
| 1 | 719 | 469.5 | — | +0.136 | 243.7 | 2204.9 | 5.19 |
| 2 | 1193 | 0.815 | 0.649 | −0.002 | 229.0 | 3.48 | 5.44 |
| 3 | 1786 | 0.593 | 0.921 | −0.030 | 209.9 | 2.21 | 5.88 |
| 4 | 2505 | 0.463 | 0.973 | −0.040 | 190.3 | 1.75 | 6.31 |
| 5 | 3324 | 0.365 | 0.988 | −0.043 | 172.1 | 1.54 | 6.68 |
| 6 | 4212 | 0.293 | 0.994 | −0.042 | 156.0 | 1.41 | 6.96 |
| 7 | 5151 | 0.242 | 0.996 | −0.039 | 142.0 | 1.28 | 7.19 |
| 8 | 6128 | 0.204 | 0.998 | −0.037 | 130.0 | 1.16 | 7.37 |

Четыре вещи, и все четыре складываются в одну историю.

**Отображение расширяющее на каждом шаге.** $\rho(J)$ не опускается ниже единицы:
3.48, 2.21, 1.75, …, 1.16. Сходится к единице сверху, но не переходит её. Значит
притягивающей неподвижной точки в смысле Банаха здесь нет вообще, и разговор про DEQ
и «сходимость к equilibrium» к этой модели просто неприменим — не потому, что она
сходится слишком быстро, а потому, что она не сжимает.

**Шаги становятся коллинеарными почти сразу.** Косинус между соседними приращениями:
0.65, 0.92, 0.97, 0.99, 0.994, 0.996, 0.998. Начиная с третьего повтора блок
воспроизводит одно и то же движение. Это дрейф по прямой, теперь на новой
архитектуре и с нормальной инициализацией.

**Каждый лишний шаг двигает состояние туда, откуда лосс растёт.** $\cos(\Delta h,
-\partial L/\partial h)$ на первом шаге $+0.14$, а дальше **отрицательный**: −0.002,
−0.030, −0.040, … То есть повтор не «бесполезен», он вреден — сдвигает против
направления, которое уменьшило бы ошибку. Ровно поэтому лосс при выходе растёт
монотонно, 5.19 → 7.37.

**Ранг падает.** 243.7 → 130.0: половина занятых направлений схлопывается за восемь
повторов. Дрейф идёт в узкое подпространство.

#### Один проход против двух из одного и того же состояния

Это прямой ответ на вопрос «что вообще покупает второй проход». Берём состояние
$h_t$, делаем из него один шаг и два шага и сравниваем сами смещения, а не разные
прогоны.

| из шага | $\cos(\Delta_1, \Delta_2)$ | $\lVert\Delta_2\rVert/\lVert\Delta_1\rVert$ | $\mathrm{KL}(p_2\|p_1)$ | лосс за 1 | лосс за 2 | совпадение top-1 |
|---|---|---|---|---|---|---|
| 0 | 0.926 | 1.65 | 119.5 | 5.19 | 5.44 | 0.60 |
| 1 | 0.976 | 2.16 | 72.4 | 5.44 | 5.88 | 0.67 |
| 2 | 0.992 | 2.15 | 41.8 | 5.88 | 6.31 | 0.70 |
| 3 | 0.997 | 2.10 | 22.2 | 6.31 | 6.68 | 0.74 |
| 4 | 0.998 | 2.06 | 12.1 | 6.68 | 6.96 | 0.78 |
| 5 | 0.999 | 2.04 | 7.0 | 6.96 | 7.19 | 0.84 |
| 6 | 0.999 | 2.03 | 4.4 | 7.19 | 7.37 | 0.88 |

Читается так: **второй проход — это повторение первого, вдвое длиннее, в сторону,
где хуже.** Направление то же ($\cos \to 0.999$), длина ровно удваивается
($\lVert\Delta_2\rVert/\lVert\Delta_1\rVert \approx 2$), лосс всегда выше.
Предсказание при этом реально меняется — top-1 переворачивается у 40% токенов на
раннем шаге и у 12% на позднем, — но каждый такой переворот в среднем в минус.

И тут же — **определение того, как должен выглядеть полезный луп**, которое можно
сделать целью метода:

| | сейчас | что нужно |
|---|---|---|
| $\cos(\Delta_1, \Delta_2)$ | $\to 0.999$ | заметно меньше 1: второй шаг **исправляет** первый, а не продолжает |
| $\lVert\Delta_2\rVert/\lVert\Delta_1\rVert$ | $\approx 2$ | меньше 2: приращения затухают, а не складываются |
| лосс за 2 против лосса за 1 | всегда выше | ниже хотя бы на части токенов |

Это не «метрика ради метрики»: все три величины считаются из одного прогона без
разметки, и по ним можно смотреть, помогает ли конкретная модификация, не дожидаясь
конца обучения.

#### Ловушка: относительный шаг врёт про сходимость

$\lVert\Delta h\rVert/\lVert h\rVert$ падает 0.81 → 0.20, и это выглядит как
сходимость. Но абсолютный шаг за те же восемь повторов **растёт**: $0.815\times 719
= 586$ на втором шаге против $0.204\times 5151 = 1050$ на восьмом. Относительный шаг
падает только потому, что норма растёт быстрее шага.

То же самое с $\mathrm{KL}(p_t \| p_{\text{финал}})$: она уходит в ноль, предсказание
перестаёт меняться — но лосс при этом растёт. Предсказание сходится, только сходится
оно к **неправильному** распределению.

Вывод для метода: **нормализованные величины ($\lVert\Delta h\rVert/\lVert h\rVert$,
KL до финала) как признак сходимости здесь не годятся**, и ранний выход, построенный
на них, остановится ровно тогда, когда состояние окончательно уехало. Признак,
который не врёт, — знак $\cos(\Delta h, -\partial L/\partial h)$, но при инференсе
градиента нет, и это отдельная задача: чем его заменить.

## Важно: шум прогона съедает все различия, которые я обсуждал

`loop4` и `loop4_traj` — **один и тот же конфиг**: тот же сид, те же данные, те же
гиперпараметры, различаются только тег и частота логирования. Результат:

| прогон | ppl |
|---|---|
| `loop4` | 1245.7 |
| `loop4_traj` | 1197.7 |

**Разброс между двумя запусками одного и того же — 48 ppl, около 4%.** Причина в
недетерминированности MPS (порядок редукций на GPU не фиксирован).

Отсюда следует неприятное: **инъекция входа дала 1197.5 против 1246, то есть 49 —
ровно на уровне этого шума. Результат ничего не доказывает.** Я подавал его как
слабое, но улучшение; правильная формулировка — «различия не обнаружено».

Второе: я раньше записал `loop8` как 2121. Это значение из середины обучения, а не
итог; итоговое **1201**. Ошибка чтения лога.

**Что теперь обязательно:**

1. Любое сравнение вариантов — минимум 3 сида, иначе разница меньше 100 ppl ничего
   не значит.
2. Для финальных чисел фиксировать детерминизм или считать на CPU/CUDA.
3. Проверить заново: нормализация (2646 против 1246) — разрыв в 1400, это заведомо
   больше шума, вывод остаётся. А вот `loop8` (1201) против `loop4` (1246) — внутри
   шума, то есть **«8 лупов хуже 4» я тоже утверждал зря**.

Исправленная таблица:

| вариант | ppl | вывод |
|---|---|---|
| `dense1` (1 проход) | 1019 | заметно лучше всех лупнутых |
| `loop4` / `loop4_traj` | 1246 / 1198 | это один конфиг, разброс 4% |
| `loop8` | 1201 | от loop4 не отличается |
| `loop4_inject` | 1197 | от loop4 не отличается |
| `loop4_norm` | 2646 | **хуже, разрыв вне шума** |
| `loop8_norm` | 2633 | то же |

Достоверных выводов ровно два: обычная модель лучше лупнутой при равном бюджете
токенов, и нормализация между лупами всё ломает. Всё остальное — шум.


## Отсев идей: первые семь из двенадцати (2026-08-20)

8M токенов, 4 лупа, один сид, всё остальное одинаково. Это не финальные числа — это
ответ на вопрос «шевелится или нет».

| идея | val ppl | против базовой |
|---|---|---|
| **`add`** — инъекция сложением | **326.9** | **−9.7%** |
| `group` — обход группами $(f_1f_2)(f_1f_2)(f_3f_4)(f_3f_4)$ | 356.0 | −1.7% |
| `base` — чистое повторение стека (архитектура Ouro) | 362.1 | — |
| `step` — эмбеддинг номера шага | 403.9 | +11.5% |
| `inject` — конкатенация с адаптером (Huginn) | 529.1 | **+46.1%** |
| `norm` — нормализация между лупами | 2002.8 | **+453%** |

### Сложение бьёт конкатенацию, и это не опечатка

Инъекция входа **сложением** даёт лучший результат из всего опробованного, а честная
конкатенация с адаптером из Huginn — почти вдвое хуже базовой строчки.

Правдоподобное объяснение: адаптер $A:\mathbb R^{2h}\to\mathbb R^h$ инициализирован
случайно, и на старте он не пропускает состояние, а перемешивает его с эмбеддингом в
случайной пропорции. Сложение работает сразу, без параметров и без обучения. За 8M
токенов адаптер выучиться не успевает; Huginn обучается на несопоставимо большем
бюджете, где эта цена окупается.

**Проверяемое следствие:** если инициализировать адаптер так, чтобы он на старте давал
ровно сложение (веса $[I; I]$), разрыв должен исчезнуть. Это одна строчка и один прогон.

### Нормализация между лупами ломает всё

2002.8 против 362.1 — разрыв в пять с половиной раз, вне всякого шума. Тот же вывод был
на старой архитектуре, теперь подтверждён на честной. Причина понятна из диагностики:
`‖h‖` растёт по лупам, и это **носитель сигнала**, а не паразитный эффект;
нормализация его стирает.


## Отсев закончен: двенадцать идей, три выживших (2026-08-21)

8M токенов, 4 лупа, один сид, всё остальное одинаково. Не финальные числа — ответ на
вопрос «шевелится или нет».

| идея | val ppl | против базовой | применений блока |
|---|---|---|---|
| **`prelude`** — 2 слоя в цикле, по одному вне | **294.8** | **−18.6%** | **10** |
| **`ouro`** — голова остановки с энтропийным регуляризатором | **297.2** | **−18.0%** | 16 |
| **`huginn`** — конкатенация + лог-нормаль + усечённый бэкпроп + prelude/coda | **322.5** | −10.9% | **10** |
| `add` — инъекция сложением | 326.9 | −9.7% | 16 |
| `layer` — обход по слоям $f_1f_1f_2f_2$ | 343.0 | −5.3% | 16 |
| `deep` — глубокий надзор | 343.7 | −5.1% | 16 |
| `group` — обход группами | 356.0 | −1.7% | 16 |
| `progress` — голова предсказания пользы шага | 360.1 | −0.6% | 16 |
| `base` — чистое повторение стека (Ouro) | 362.1 | — | 16 |
| `step` — эмбеддинг номера шага | 403.9 | +11.5% | 16 |
| `ponder` — та же голова, но регуляризатор PonderNet | 416.0 | +14.9% | 16 |
| `inject` — конкатенация с адаптером | 529.1 | +46.1% | 16 |
| `norm` — нормализация между лупами | 2002.8 | +453% | 16 |

### Победитель делает меньше работы, а не больше

`prelude` лучший, и при этом он тратит **10 применений блока против 16** у базовой — 62%
компьюта. То есть выигрыш не куплен вычислениями: **вынести часть слоёв из цикла лучше,
чем крутить весь стек.**

Это прямо противоречит наивному чтению условия «чем больше лупов, тем лучше» и
согласуется с устройством Huginn, где повторяется только core. Объяснение, которое я
считаю основным: первый слой должен превратить эмбеддинги в состояние, а последний —
состояние в предсказание; заставлять одни и те же веса делать это и одновременно
итерировать — противоречивые требования, и разделение их снимает.

Оговорка честная: сравнение при равном числе токенов, но **не при равном компьюте**. Надо
догнать `prelude` по применениям блока (2 слоя × 7 лупов + 2 = 16) и посмотреть, растёт
ли выигрыш дальше.

### Регуляризатор головы остановки решает всё

`ouro` 297.2 против `ponder` 416.0 — **одна и та же голова, одна и та же архитектура,
разница только в том, чем регуляризуют распределение остановки**: энтропией самого
распределения (Ouro) против KL к геометрическому приору (PALBERT/PonderNet). Разрыв 29%.

Это довод в пользу того, что фиксированный геометрический приор — плохая идея: он
навязывает, на каком шаге выходить, вместо того чтобы просто не давать распределению
схлопнуться.

### Что разложилось по частям

`huginn` (322.5) хуже, чем просто `prelude` (294.8), при том же компьюте. Значит внутри
комбинации Huginn **работает вынос слоёв из цикла**, а конкатенация с адаптером,
лог-нормальное число повторов и усечённый бэкпроп на этом бюджете **стоят денег**. Это
согласуется с тем, что видно по `inject` отдельно: адаптер не успевает обучиться.

### На A100 везём

1. `prelude`, догнанный по компьюту, — главный кандидат.
2. `ouro` — второй, и он ортогонален первому: их надо попробовать вместе.
3. `add` — почти бесплатное улучшение, стоит оставить включённым.

Отброшены с объяснением: `norm`, `inject`, `ponder`, `step`.
